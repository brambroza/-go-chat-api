exports.verifySignature = (headers, body) => {
    // Logic สำหรับตรวจสอบ signature ตามที่ TikTok กำหนด
    // เช่น headers['x-tiktok-signature'] เทียบกับ secret
    return true;
  };
  
  exports.doSomething = (data) => {
    // Logic ประมวลผลข้อมูล
  };
  